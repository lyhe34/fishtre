<li {{ attributes }} class="color-blue">
    {% if this.cartProducts|length == 0 %}
        <p>Votre panier est vide</p>
    {% else %}
        {% for cartProduct in this.cartProducts %}
            <ul>
                {{ component('CartProductCard', { cartProduct: cartProduct }) }}
            </ul>
        {% endfor %}
    {% endif %}
</li>